new Mem.Rule("winner").schema ->
  @scope (all)->
    shows: ->
      all.where (o)->
        o.name != '―'

  @default ->

  @deploy (o)->

  @map_reduce (o)->

Mem.rule.winner.set <%= JSON.generate SET_WIN %>
